export default function CourseAdminCourses({ userId }: { userId: string | null }) {
    return (
        <div className="">
            <h1 className="">
                Course Admin Courses
            </h1>
        </div>
    );
}